---
title: "stack-test"
output: BiocStyle::html_document
vignette: >
  %\VignetteIndexEntry{stack-test}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
    collapse = TRUE,
    comment = "#>",
    warning = FALSE,
    message = FALSE
)
```

```{r setup}
library(phylobar)
library(ape)
```

In this step ...

```{r, read_in_data}
ps <- readRDS("vignettes/zhang2023-ps.rds")
x_mat <- subset_cluster(t(otu_table(ps)))
x_mat <- x_mat / rowSums(x_mat)
```

```{r}
phylobar(x_mat, phy_tree(ps), width = 900, sample_show_all = FALSE)
```

```{r}
sessionInfo()
```